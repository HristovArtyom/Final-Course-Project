import './hero.scss';
import Slider from '../Swiper/Swiper';

const Hero = () => {
    return (
    <section className={`hero`}>
        <Slider>
        </Slider>
    </section>
  )
}

export default Hero;